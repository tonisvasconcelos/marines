# Use Node.js LTS version
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production

# Copy application code
COPY . .

# Expose port (Vercel will set PORT automatically)
EXPOSE 3001

# Start the application
CMD ["node", "server.js"]

